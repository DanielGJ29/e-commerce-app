const initialState = {
  name: "Daniel",
  lastName: "Gonzalez Jimenez",
  age: 31,
};
export const userReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
